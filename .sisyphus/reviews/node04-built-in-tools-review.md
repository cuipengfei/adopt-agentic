# Review: 节点 4 — 内置工具 (Built-in Tools)

## 总评
这版 first pass **结构合格、主干正确**，但还有 3 个关键改进点：**JSON 示例一致性、中文与英文对等性、与 MCP 的收尾衔接**。
节点 4 已经达到"可读可用"的 first pass 水位。建议先修 3 个点：`tool_calls` 示例字段闭环、中文补齐"工具=上下文"显式段落、结尾补一段通向 MCP 的桥接。不需要重写，做一次 focused revision 即可。

★ Insight：这个节点最有价值的句子已经在文档里了——**"工具定义是行动说明书，工具返回值是感知世界的方式"**。下一版只要围绕这句话做结构和示例闭环，质量会立刻上一个台阶。

## 逐项评估

| 维度 | 结论 | 建议 |
|---|---|---|
| 1) 写作风格 | **部分达标** | 保留锋利句，删解释性套话，句子再短一点 |
| 2) 精简度 | **可再压缩** | 目标压缩 10-15%，优先删"重复解释本地执行" |
| 3) 上下文主线 | **达标但不够突出** | 中文补一段与英文 119-126 行等价内容 |
| 4) HTTP/SSE 模式 | **基本达标，细节有洞** | 示例里补齐 `id`（或移除 `tool_call_id`），字段前后一致 |
| 5) 工具调用流程 | **达标** | 可在小标题里显式写成 4 步 |
| 6) 信任边界 | **达标** | 保持轻提 |
| 7) Agent Agnostic | **达标** | |
| 8) 中英文质量对等 | **未完全对等** | 两边做 1:1 结构对齐 |
| 9) 横切关注点 | **达标** | |
| 10) 前后节点衔接 | **前承好，后启弱** | 结尾补通向 MCP 的桥接 |

## 具体修改建议
- [ ] 先修示例协议一致性：`tool_calls[].id` 与 `tool_call_id` 必须闭环。
- [ ] 中文新增"工具的双重上下文角色"小节，与英文对应。
- [ ] 中英文做结构对齐检查：标题层级、段落数量、关键术语一一对应。
- [ ] 压缩冗余句（重点 64-67、117、130 一带），只保留信息增量句。
- [ ] 在末尾补"下一节 MCP"桥接段，明确"内置 vs 外接"边界。
- [ ] 最后跑一次"扫读测试"：是否能在 60 秒内看懂 4 步调用链。
