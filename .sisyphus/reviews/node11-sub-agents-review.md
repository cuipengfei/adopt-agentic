# Review: 节点 11 — Sub Agent — 上下文隔离

## 总评
整体质量高、主线正确、中英文基本对等。当前主要短板有 3 个：**HTTP/SSE 机制展示不够完整（缺第 2 轮增长示例）**、**与 orchestration/eval 的前后衔接偏弱**、**示例细节有小瑕疵（步骤编号 1→3）**。
建议做一轮小幅精修即可进入 next pass。

> ★ Insight
> 这篇最强的地方是"隔离=创造全新上下文"讲清了；最该补的是"隔离之后如何验证摘要可靠性"，这正好是通往下一章 Eval 的桥。

## 逐维度评审

1. **写作风格（十人混血儿）**：**通过**
   表达有力度、技术密度够。可再削掉 1-2 句比喻性重复。

2. **精简度**：**基本通过**
   信息密度高，但"clean room / expert meeting / git stash"三重类比略重。保留两种即可。

3. **上下文主线：创造全新上下文（隔离）**：**通过**
   定义准确，和第一原则一致。

4. **为什么需要隔离——上下文污染**：**通过**
   问题定义清晰，和 `context.md` 的"污染"段落逻辑闭环良好。

5. **工作方式：fork → 独立执行 → 摘要回注**：**基本通过**
   结构完整，但代码注释编号缺"2"（1→3）。建议明确"fork 是本地调度动作，不是 LLM API 调用"。

6. **回扣第一原则：初始 prompt 质量决定效果**：**通过**
   三点标准（独立/完备/聚焦）可直接落地。

7. **Agent Agnostic**：**通过**

8. **中英文质量对等**：**通过（高）**
   近乎逐句对齐。英文可微调 "contamination"→"pollution" 语域统一。

9. **横切关注点齐全**：**通过**
   尤其"摘要可疑时下钻子会话"很实用。

10. **与前后节点衔接**：**部分通过**
   目前主要回扣了 context。与 orchestration/eval 的显式桥接还不够。

## 具体修改建议
- [ ] 在 "Inside the Sub Agent" 增加**第 2 轮 REQUEST/RESPONSE**，显式展示 `messages` 变长。
- [ ] 修正代码示例步骤编号（1→2→3），并补一句：`createSubAgent(...)` 是本地编排动作。
- [ ] 压缩类比数量（保留"clean room + one technical analogy"即可）。
- [ ] 新增 1 段"与上一章 Orchestration 的衔接"：并行/串行仍不够时，用 Sub Agent 做上下文隔离。
- [ ] 新增 1 段"通往下一章 Eval 的衔接"：Sub Agent 返回的是摘要，不是真相；摘要需要验证。
- [ ] 中英同步微调措辞，保持术语一致。
- [ ] 明确"运行时隔离 vs 日志可审计"不冲突。
